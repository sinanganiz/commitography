// Package storage holds persistence: the storage interface and its embedded
// implementation (ADR-0017, ADR-0035). It is empty until the storage packages
// introduce it; the layout reserves its place (ADR-0040 clause 1).
package storage
