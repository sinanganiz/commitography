# Build output
/out/
/ci-out/
/dist/
/commitography
/commitography.exe

# Go
*.test
*.out

# Frontend. The built bundle under internal/render/assets/ is deliberately
# committed: //go:embed needs it, so `go build ./...` must work on a clean
# checkout without Node installed. `make web` regenerates it.
web/node_modules/
web/dist/

# Test fixtures are generated, not committed
/testdata/fixtures/*
!/testdata/fixtures/.gitkeep

# Editors and OS
.DS_Store
Thumbs.db
.idea/
.vscode/
